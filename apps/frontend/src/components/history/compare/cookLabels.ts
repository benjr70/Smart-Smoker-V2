/**
 * What the comparison calls a cook nobody named.
 *
 * One spelling, in one place: the slot card, the picker row and the verdict all
 * name the same nameless cook, and three variants of it on one screen read as
 * three different cooks.
 */
export const UNNAMED_COOK = 'Unnamed cook';
