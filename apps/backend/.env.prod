# Production Cloud Environment Configuration
# Actual secrets are injected via GitHub Secrets in CI/CD workflow

# MongoDB Connection with Authentication
# Format: mongodb://username:password@host:port/database?authSource=admin
# Password is injected from MONGO_APP_PASSWORD environment variable
DB_URL=mongodb://smartsmoker:${MONGO_APP_PASSWORD}@mongo:27017/smartsmoker?authSource=admin