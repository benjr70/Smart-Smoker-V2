{
  "name": "@smart-smoker/smoke",
  "version": "0.1.0",
  "private": true,
  "description": "Post-deploy smoke test (Playwright + health probes). Part of PRD #183 Level 6 harness.",
  "type": "module",
  "scripts": {
    "smoke": "tsx run.ts",
    "smoke:install": "playwright install --with-deps chromium"
  },
  "dependencies": {
    "playwright": "^1.49.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
